import React from "react";
import { Header, Menu } from "semantic-ui-react";

export default function ActivityFilters () {
    return (
        <Menu size="large" vertical style={{width:'100%'}}>
            <Header icon='filter' attached color="tea"
        </Menu>
    )
}